{
  "name": "braingames",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "postinstall": "ECHO you must copy node_modules/electron-prebuilt/dist to dist to use npm start",
    "prestart": "asar pack app dist/resources/app.asar",
    "start": ".\\dist\\electron.exe"
  },
  "devDependencies": {
    "asar": "^0.9.0",
    "electron-prebuilt": "^0.36.2"
  }
}
